#include "main.h"
/**
 * _isupper - prints the 9 times table, starting with 0
 *
 * @c: number to be tested
 *
 * Return: 1 if c is uppercase else 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	return (0);
}
