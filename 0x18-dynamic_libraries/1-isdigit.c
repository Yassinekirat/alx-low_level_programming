#include "main.h"
/**
 * _isdigit - prints the 9 times table, starting with 0
 *
 * @c: number to be tested
 *
 * Return: 1 if c is a digit else 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	return (0);
}
