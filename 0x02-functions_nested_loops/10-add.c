#include "main.h"

/**
 * add - adds two integers and returns the result
 *
 * @x: first number
 * @z: second number
 *
 *
 * Return: x + z
 */
int add(int x, int z)
{
	int result;

	result = x + z;

	return (result);
}
